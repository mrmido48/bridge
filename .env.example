<<<<<<< HEAD
# SnopDesk WhatsApp Bridge
# Copy to .env and set values.

# Full URL of your SnopDesk app (used to call /api/whatsapp/connected and /api/whatsapp/incoming)
PHP_BASE_URL=http://localhost:8000

# Must match WHATSAPP_WEBHOOK_SECRET or APP_KEY in SnopDesk .env
WEBHOOK_SECRET=

# Optional: port for this bridge (default 3000)
# PORT=3000

# Optional: folder for WhatsApp session data (default: ./.wwebjs_auth in bridge dir).
# Set to a persistent path on production (e.g. /var/lib/snopdesk/whatsapp-auth) so login survives restarts and deploys.
# AUTH_DATA_PATH=
=======
# SnopDesk WhatsApp Bridge
# Copy to .env and set values.

# Full URL of your SnopDesk app (used to call /api/whatsapp/connected and /api/whatsapp/incoming)
PHP_BASE_URL=http://localhost:8000

# Must match WHATSAPP_WEBHOOK_SECRET or APP_KEY in SnopDesk .env
WEBHOOK_SECRET=

# Optional: port for this bridge (default 3000)
# PORT=3000

# Optional: folder for WhatsApp session data (default: ./.wwebjs_auth in bridge dir).
# Set to a persistent path on production (e.g. /var/lib/snopdesk/whatsapp-auth) so login survives restarts and deploys.
# AUTH_DATA_PATH=
>>>>>>> d03ea09c721cbf5be7e1d72fa95ea734eba5cd22
